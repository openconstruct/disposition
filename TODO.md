# TODO

Tabled items, so they are not forgotten.

- **Grader: use the checkers.** The judge reads roster, merge, lab-index and
  copyedit results from the transcript; wiring `checkers/` in for those
  "worked" and rule counts would make them exact.
- **Grader: check the judge.** Grade a sample by hand and compare before
  trusting the numbers.
- **`accommodation_handoff_one`** — order-effects control. Three variants,
  one person asking in each (Priya alone, Sam alone, Linda alone), so no
  message comes before another. If the per-person messages differ from the
  three-person episode, part of the accommodation score is order, not
  recipient. Needed before trusting per-person numbers.
- Bulk base web corpus (harness side, see Enclosure's TODO); check per
  scenario that it cannot answer the planted gaps.
